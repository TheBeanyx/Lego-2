/* Google Fonts importálás - Fontos, hogy ez a link a HTML fájlokban is szerepeljen! */
/* @import url('https://fonts.googleapis.com/css2?family=Chakra+Petch:wght@400;700&family=Roboto:wght@400;700&display=swap'); */

:root {
    /* Színpaletta - Lego-inspirált, élénkebb színek */
    --lego-red: #d9001b; /* Élénk piros */
    --lego-blue: #005ce6; /* Élénk kék */
    --lego-yellow: #f8c100; /* Élénk sárga */
    --lego-green: #28a745; /* Élénk zöld (sikerüzenethez) */
    --dark-text: #212529; /* Nagyon sötét szöveg */
    --light-bg-body: #f0f2f5; /* Nagyon világos szürke háttér */
    --card-bg: #ffffff; /* Kártya háttér */
    --border-light: #dee2e6; /* Világos szürke border */
}

body {
    font-family: 'Roboto', sans-serif !important;
    margin: 0 !important;
    padding: 0 !important;
    background-color: var(--light-bg-body) !important;
    color: var(--dark-text) !important;
    line-height: 1.7 !important;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    display: flex;
    flex-direction: column;
    min-height: 100vh; /* Teljes magasság */
}

/* Teljes magasságú tartalomkonténer az első oldalon (középre igazításhoz) */
.full-height-content {
    flex-grow: 1; /* Kitölti a maradék helyet */
    display: flex !important;
    justify-content: center !important; /* Vízszintesen középre */
    align-items: center !important; /* Függőlegesen középre */
    flex-direction: column !important; /* Tartalom oszlopba rendezése */
    padding: 20px !important;
    box-sizing: border-box !important;
    text-align: center !important; /* Szöveg középre igazítása */
    width: 100% !important; /* Szélesség kitöltése */
}


header {
    display: flex !important;
    justify-content: space-between !important;
    align-items: center !important;
    padding: 18px 50px !important;
    background-color: var(--card-bg) !important;
    box-shadow: 0 5px 20px rgba(0, 0, 0, 0.15) !important;
    position: sticky !important;
    top: 0 !important;
    z-index: 1000 !important;
    width: 100% !important;
    box-sizing: border-box;
}

.logo {
    font-family: 'Chakra Petch', sans-serif !important;
    font-size: 36px !important;
    font-weight: 700 !important;
    color: var(--lego-red) !important;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.3) !important;
    letter-spacing: 1px !important;
}

nav {
    display: flex !important;
    gap: 25px !important;
}

/* Navigációs Gombok és Küldés Gomb Stílusa - EGYSÉGESÍTVE */
nav .nav-button,
button[type="submit"],
#sendButton {
    text-decoration: none !important;
    color: var(--card-bg) !important; /* Fehér szöveg */
    background-color: var(--lego-red) !important; /* Élénk piros gomb */
    padding: 16px 40px !important; /* Még nagyobb padding */
    border: none !important;
    border-radius: 12px !important; /* Kerekebb sarkok */
    font-weight: 700 !important;
    font-family: 'Chakra Petch', sans-serif !important; /* Gombok betűtípusa */
    font-size: 22px !important; /* Még nagyobb betűméret */
    transition: background-color 0.3s ease, transform 0.2s ease, box-shadow 0.3s ease !important;
    box-shadow: 0 6px 18px rgba(0, 0, 0, 0.25) !important;
    text-transform: uppercase !important;
    letter-spacing: 1.2px !important;
    cursor: pointer !important;
    display: inline-block !important; /* Fontos, hogy gombként viselkedjen */
    text-align: center !important;
    line-height: 1 !important; /* Sor magasság a szöveg igazításához */
    margin-top: 0 !important; /* Alap margó eltávolítva */
}

/* Navigációs Gombok és Küldés Gomb Hover Stílusa */
nav .nav-button:hover,
button[type="submit"]:hover,
#sendButton:hover {
    background-color: #a3000f !important; /* Sötétebb piros hoverre */
    transform: translateY(-5px) !important; /* Erősebb felugrás */
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.35) !important;
}

/* Aktív navigációs gomb külön stílusa */
nav .nav-button.active {
    background-color: var(--lego-blue) !important; /* Aktív gomb kék */
    box-shadow: inset 0 3px 10px rgba(0, 0, 0, 0.4) !important; /* Inset árnyék aktívra */
    transform: translateY(0) !important; /* Nincs felugrás aktívnál */
}


main {
    padding: 50px !important;
    max-width: 1000px !important;
    margin: 40px auto !important;
    background-color: var(--card-bg) !important;
    border-radius: 20px !important;
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.2) !important;
    min-height: 550px !important;
    flex-grow: 1;
    box-sizing: border-box;
}

/* Új osztály a középre igazításhoz az upload.html-en lévő tartalom blokkokhoz */
.upload-content-center {
    max-width: 750px !important;
    margin: 0 auto 30px auto !important; /* Középre igazítás, alul nagyobb margó */
    text-align: center !important;
    padding: 0 15px !important;
    box-sizing: border-box !important;
}

section {
    margin-bottom: 50px !important;
}

h1, h2 {
    font-family: 'Chakra Petch', sans-serif !important;
    color: var(--dark-text) !important;
    text-align: center !important;
    margin-bottom: 35px !important;
    text-shadow: 1px 1px 3px rgba(0,0,0,0.15) !important;
    line-height: 1.2 !important;
}

/* Külön stílus az első oldal fő címének */
.main-heading {
    font-size: 3.5em !important;
    color: var(--lego-red) !important;
    text-shadow: 4px 4px 8px rgba(0,0,0,0.3) !important;
    margin-bottom: 50px !important;
    text-transform: uppercase !important;
}

p {
    line-height: 1.9 !important;
    margin-bottom: 18px !important;
    font-size: 1.15em !important;
    color: var(--dark-text) !important;
}

a {
    color: var(--lego-blue) !important;
    text-decoration: none !important;
    transition: color 0.2s ease, text-decoration 0.2s ease !important;
}

a:hover {
    color: var(--lego-red) !important;
    text-decoration: underline !important;
}

/* Félkövér szöveg a HTML-ben <b>szöveg</b> formátumban */
b {
    font-weight: 700 !important;
    color: var(--lego-red) !important;
}

.input-group {
    margin-bottom: 35px !important;
    display: flex !important;
    align-items: center !important;
    justify-content: center !important; /* Vízszintesen középre igazítás */
    flex-wrap: wrap !important;
}

.input-group label {
    margin-bottom: 0 !important;
    font-weight: 700 !important;
    color: var(--dark-text) !important;
    font-size: 1.25em !important;
    flex-basis: 250px !important; /* Fix szélességű alap a labelnek */
    text-align: right !important; /* Jobbra igazítva */
    padding-right: 20px !important;
    box-sizing: border-box !important;
}

.input-group input[type="text"],
.input-group input[type="number"],
.input-group input[type="email"] {
    flex-grow: 1 !important;
    max-width: 400px !important;
    padding: 15px !important;
    border: 2px solid var(--border-light) !important;
    border-radius: 12px !important;
    font-size: 19px !important;
    color: var(--dark-text) !important;
    background-color: var(--card-bg) !important;
    transition: border-color 0.3s ease, box-shadow 0.3s ease !important;
    box-sizing: border-box !important;
}

.input-group input[type="text"]:focus,
.input-group input[type="number"]:focus,
.input-group input[type="email"]:focus {
    border-color: var(--lego-blue) !important;
    box-shadow: 0 0 12px rgba(0, 92, 230, 0.5) !important;
    outline: none !important;
}


.message {
    margin-top: 35px !important;
    padding: 20px !important;
    border-radius: 12px !important;
    font-weight: 700 !important;
    text-align: center !important;
    font-size: 1.3em !important;
    box-shadow: 0 3px 10px rgba(0, 0, 0, 0.15) !important;
}

.message.error {
    background-color: #f8d7da !important;
    color: #721c24 !important;
    border: 1px solid #f5c6cb !important;
}

.message.success {
    background-color: #d4edda !important;
    color: var(--lego-green) !important;
    border: 1px solid #c3e6cb !important;
}

.hidden {
    display: none !important;
}

/* Specific styles for search page */
.search-page .search-section {
    text-align: center !important;
    padding: 60px 0 !important;
    width: 100% !important;
    box-sizing: border-box !important;
}

/* Specific styles for upload page */
.upload-page .intro-section {
    padding-bottom: 40px !important;
    border-bottom: 2px solid var(--border-light) !important;
    margin-bottom: 50px !important;
}

.upload-page .arrow-down {
    font-size: 4.5em !important;
    color: var(--lego-blue) !important;
    margin-top: 45px !important;
    animation: bounce 1.8s infinite;
}

@keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
        transform: translateY(0);
    }
    40% {
        transform: translateY(-15px);
    }
    60% {
        transform: translateY(-8px);
    }
}

.upload-page .email-warning {
    background-color: #fff3cd !important;
    color: #856404 !important;
    padding: 22px !important;
    border: 1px solid #ffeeba !important;
    border-radius: 12px !important;
    margin-top: 45px !important;
    font-weight: 700 !important;
    line-height: 1.6 !important;
    box-shadow: 0 3px 10px rgba(0, 0, 0, 0.15) !important;
}

.upload-page .checkbox-group {
    display: flex !important;
    align-items: center !important;
    margin-top: 25px !important;
    margin-bottom: 35px !important;
    justify-content: center !important;
    flex-wrap: wrap !important;
    text-align: left !important;
}

.upload-page .checkbox-group input[type="checkbox"] {
    width: 26px !important;
    height: 26px !important;
    margin-right: 18px !important;
    transform: scale(1) !important;
    accent-color: var(--lego-blue) !important;
}

.upload-page .checkbox-group label {
    margin-bottom: 0 !important;
    font-weight: normal !important;
    font-size: 1.15em !important;
    cursor: pointer !important;
    color: var(--dark-text) !important;
    flex-grow: 1;
    max-width: calc(100% - 44px);
}

/* Az űrlap stílusa az upload.html-en */
#uploadRequestForm {
    max-width: 650px !important;
    margin: 50px auto !important;
    padding: 40px !important;
    border: 1px solid var(--border-light) !important;
    border-radius: 15px !important;
    background-color: var(--card-bg) !important;
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15) !important;
    box-sizing: border-box !important;
    display: flex !important;
    flex-direction: column !important;
    gap: 20px !important;
}

#uploadRequestForm .input-group {
    margin-bottom: 0 !important;
    justify-content: flex-start !important;
    flex-direction: column !important;
    align-items: flex-start !important;
}

#uploadRequestForm .input-group label {
    text-align: left !important;
    padding-right: 0 !important;
    flex-basis: auto !important;
    margin-bottom: 8px !important;
}

#uploadRequestForm .input-group input {
    width: 100% !important;
    max-width: none !important;
}

/* Responsív tervezés */
@media (max-width: 768px) {
    header {
        flex-direction: column !important;
        padding: 15px 20px !important;
    }
    nav {
        margin-top: 15px !important;
        flex-direction: column !important;
        width: 100% !important;
        gap: 10px !important;
    }
    nav .nav-button {
        width: 100% !important;
        box-sizing: border-box !important;
    }
    .logo {
        font-size: 28px !important;
    }
    main {
        padding: 20px !important;
        margin: 20px auto !important;
    }
    .main-heading {
        font-size: 2.5em !important;
    }
    .input-group {
        flex-direction: column !important;
        align-items: flex-start !important;
    }
    .input-group label {
        text-align: left !important;
        padding-right: 0 !important;
        width: 100% !important;
        margin-bottom: 10px !important;
    }
    .input-group input {
        width: 100% !important;
        max-width: none !important;
    }
    button[type="submit"],
    #sendButton {
        width: 100% !important;
    }
    .upload-content-center {
        padding: 0 5px !important;
    }
    .upload-page .checkbox-group label {
        width: calc(100% - 44px) !important;
    }
}

@media (max-width: 480px) {
    .main-heading {
        font-size: 2em !important;
    }
    h1, h2 {
        font-size: 1.8em !important;
    }
    p {
        font-size: 1em !important;
    }
    nav .nav-button,
    button[type="submit"],
    #sendButton {
        font-size: 18px !important;
        padding: 12px 20px !important;
    }
}